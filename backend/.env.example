# Server Configuration
PORT=4000

# Database Configuration (PostgreSQL)
# For local development:
# DATABASE_URL="postgresql://user:password@localhost:5432/searchshare?schema=public"
#
# For Vercel Postgres:
# DATABASE_URL is automatically set by Vercel when you provision a Postgres database

# DataForSEO API Credentials (optional - can also be passed per request)
DATAFORSEO_LOGIN=your_login
DATAFORSEO_PASSWORD=your_password
